<%= form_for([@board, @post]) do |f| %>
  <div class="field">
    <%= f.label '제목' %><br>
    <%= f.text_field :title %><br>
    <%= f.label '내용' %><br>
    <%= f.text_area :content %>
  </div>
  <div class="action">
    <%= f.submit '제출하기' %>
  </div>
<% end %>